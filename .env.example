# Operator credentials for the admin-ui login.
# docker-compose.yml has built-in defaults; override here when needed.
# .env (gitignored) takes precedence over docker-compose.yml defaults.
# The backend auto-creates the credential file on startup if it doesn't exist.
OPERATOR_USERNAME=operator
OPERATOR_PASSWORD=
